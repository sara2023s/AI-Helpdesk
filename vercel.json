{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/tickets/[id]/trigger/[agentId].ts": {
      "maxDuration": 300
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
